#include "mfunc.h"
#include <cmath>

int add(int x, int y)
{
	return x + y;
}

int sub(int x, int y)
{
	return x - y;
}

int multi(int x, int y)
{
	return x * y;
}

double div1(double x, int y)
{
	return x / y;
}

int pow(int x, int y)
{
	return pow(x, y);
}
